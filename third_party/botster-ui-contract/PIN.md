Vendored `botster-ui-contract` 0.3.3 for Cargo identity unification.

Source: Git tag `botster-ui-contract-v0.3.3` on trybotster/botster-hub
(`12e0cc6994be18024e4bdfffb22947526a652204`).

Kit still depends on tag `botster-ui-contract-v0.3.2`. A same-URL git `[patch]`
is rejected, so this path crate is the single identity for both tags.
