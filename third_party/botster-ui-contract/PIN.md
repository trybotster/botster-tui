# Pin provenance

Vendored copy of `botster-ui-contract` from Hub
`cb93df53d66fead323973b5233d4589562cf57b1` (byte-identical with the kit's older
Hub pin content). Used only as a Cargo `[patch]` target so `botster-tui-kit` and
Hub-pinned crates share one type identity. Do not edit; re-copy from the Hub pin
when the Hub pin advances.
